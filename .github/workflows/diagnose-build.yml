name: Diagnose Build

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/dedicated-hotkey-window
      - name: Select Xcode
        run: sudo xcode-select -s "/Applications/Xcode_$(grep '^xcode ' .tool-versions | awk '{print $2}').app"
      - name: Setup GhosttyKit
        run: scripts/setup.sh
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Capture compiler errors
        run: |
          swift build --build-tests --quiet > /tmp/build.log 2>&1 || true
          grep -E -B 8 -A 12 -n '(^error:|:[0-9]+:[0-9]+: error:)' /tmp/build.log > compile-errors.txt || true
          rm -f build-errors.txt
          if git diff --quiet -- compile-errors.txt build-errors.txt; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A compile-errors.txt build-errors.txt
          git commit -m "ci: isolate Swift compiler errors"
          git push origin HEAD:feature/dedicated-hotkey-window
